export default class Projectile {
  constructor(game, x, y, vx, vy) {
    this.image = document.querySelector("");
    this.game = game;
    this.x = x;
    this.y = y;
    this.vx = vx;
    this.vy = vy;
  }
  draw(c) {

  }
}